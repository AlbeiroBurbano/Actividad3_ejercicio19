
package Equilatero;

public class area {
    public static double Calcular_area(double lado){
        double area;
        area = (Math.sqrt(3) / 4) * Math.pow(lado, 2);
        return area;
    }
}
